# Material 3 Expressive Hyprlock Configuration
# Beautifully integrated with the active theme, dynamic fonts, and dynamic media player

# Load active theme colors and font variables
source = ~/.config/hypr/current_theme/colors.conf

# Fallback font families if undefined by the theme colors.conf
$font_family = Inter
$font_family_bold = Inter Bold

background {
    monitor =
    path = /home/levi/.config/hypr/current_wallpaper # Dynamic symlink to current wallpaper
    blur_passes = 3 # Premium glassmorphic background blur
    blur_size = 7
    noise = 0.015
    contrast = 0.89
    brightness = 0.55
    vibrancy = 0.2
    vibrancy_darkness = 0.2
}

general {
    hide_cursor = true
}

# TIME (Large Expressive M3 Clock)
label {
    monitor =
    text = cmd[update:1000] date +"%-I:%M"
    color = rgba(255, 255, 255, 1.0) # White
    font_size = 130
    font_family = $font_family_bold # Dynamically loaded from active theme colors.conf!
    position = 0, 200
    halign = center
    valign = center
}

# DATE (Elegant Subtitle)
label {
    monitor =
    text = cmd[update:1000] date +"%A, %B %d"
    color = rgba(255, 255, 255, 1.0) # White
    font_size = 36
    font_family = $font_family # Dynamically loaded from active theme colors.conf!
    position = 0, 70
    halign = center
    valign = center
}


# INPUT FIELD (Rounded capsule with M3 border and background)
input-field {
    monitor =
    size = 280, 50
    outline_thickness = 2
    dots_size = 0.25 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.3 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(255, 255, 255, 0.5) # White outline
    inner_color = rgba(0, 0, 0, 0.5) # Dark transparent background
    font_color = rgb(255, 255, 255)
    fade_on_empty = false
    rounding = 9999 # Fully circular M3 capsule shape
    check_color = rgba(255, 255, 255, 0.8)
    fail_color = rgb(243, 139, 168) # M3 Critical Red
    placeholder_text = 
    hide_input = false
    position = 0, -50
    halign = center
    valign = center
}

# CURRENT PLAYING MEDIA (Beautiful Material 3 Capsule Style)
label {
    monitor =
    text = cmd[update:1000] artist="$(playerctl metadata artist 2>/dev/null)"; title="$(playerctl metadata title 2>/dev/null)"; if [ -n "$title" ]; then if [ -n "$artist" ]; then echo "󰎆  $title - $artist"; else echo "󰎆  $title"; fi; fi
    color = rgba(255, 255, 255, 0.9) # White
    font_size = 16
    font_family = $font_family_bold
    position = 0, -130
    halign = center
    valign = center
}
